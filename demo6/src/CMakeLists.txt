FIND_PACKAGE(HELLO REQUIRED)

IF(HELLO_FOUND)
    ADD_EXECUTABLE(demo6 main.c)
    INCLUDE_DIRECTORIES(${HELLO_INCLUDE_DIR})
    TARGET_LINK_LIBRARIES(demo6 ${HELLO_LIBRARY})
ELSE (HELLO_FOUND)
    MESSAGE(STATUS "HELLO IS NOT FOUND!")
ENDIF (HELLO_FOUND)

